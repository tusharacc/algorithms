import random

MAX_VALUE = 100000

class Node:
	def __init__(self, value = None):
		self.value = value
		self.next = None

class LinkedList:
	def __init__(self):
		self.head = None

	def append(self,data):
		node = Node(data)
		if self.head:
			self.
			self.next


ll = LinkedList()

for i in range(MAX_VALUE):
	pass